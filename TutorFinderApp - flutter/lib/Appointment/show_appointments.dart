import 'package:flutter/material.dart';

class ShowAppointments extends StatelessWidget {
  const ShowAppointments({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
